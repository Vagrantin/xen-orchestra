--- a/packages/xo-web/src/xo-app/menu/index.js
+++ b/packages/xo-web/src/xo-app/menu/index.js
@@ -36,6 +36,15 @@
 
 import styles from './index.css'
 
+// Customisation — set to true to hide the corresponding menu section
+const HIDDEN_MENU_ITEMS = {
+  hub: true,
+  xoa: true,
+  proxies: true,
+  xostor: true,
+  noSupport: true,
+}
+
 const LINK_STYLE = {
   display: 'flex',
 }
@@ -348,7 +357,7 @@
           },
         ],
       },
-      {
+      !HIDDEN_MENU_ITEMS.xoa && {
         to: isAdmin ? 'xoa/update' : 'xoa/notifications',
         icon: 'menu-xoa',
         label: 'xoa',
@@ -457,7 +466,7 @@
           },
         ],
       },
-      isAdmin && {
+      isAdmin && !HIDDEN_MENU_ITEMS.hub && {
         to: '/hub/templates',
         icon: 'menu-hub',
         label: 'hubPage',
@@ -474,7 +483,7 @@
           },
         ],
       },
-      isAdmin && {
+      isAdmin && !HIDDEN_MENU_ITEMS.proxies && {
         to: '/proxies',
         icon: 'proxy',
         label: 'proxies',
@@ -502,7 +511,7 @@
         label: 'taskMenu',
         pill: nResolvedTasks,
       },
-      isAdmin && {
+      isAdmin && !HIDDEN_MENU_ITEMS.xostor && {
         to: '/xostor',
         label: 'xostor',
         icon: 'menu-xostor',
@@ -609,7 +618,7 @@
               </Link>
             </li>
           )}
-          {(isAdmin || +process.env.XOA_PLAN === 5) && (
+          {!HIDDEN_MENU_ITEMS.noSupport && (isAdmin || +process.env.XOA_PLAN === 5) && (
             <li className='nav-item xo-menu-item'>
               <Link className='nav-link' style={{ display: 'flex' }} to='/about'>
                 {+process.env.XOA_PLAN === 5 ? (
